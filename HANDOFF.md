# 引き継ぎ資料（GitHub + Supabase + クラウド運用への移行）

最終更新: 2026-09-11

このファイルは、これまでこの作業環境（Netlify 上のエージェント実行）で進めてきた
内容を、**GitHub リポジトリ + Supabase + クラウド版の Claude Code** という体制で
そのまま続けられるようにするための引き継ぎ資料です。人が読む運用手順と、
エージェントが読む前提条件の両方をここに集約しています。

- アプリの設計・スキーマの実態・実装上の注意 → `AGENTS.md`（これが技術的な正典）
- アプリの概要・ローカル起動 → `README.md`
- Supabase への SQL 適用手順 → `supabase/README.md`
- 引き継ぎ・運用・未対応事項 → このファイル

---

## 1. いまどうなっているか（現状）

### できあがっているもの

- 学生画面（`?token=<個人トークン>`）
  クール表形式のタイムテーブル、枠のタップで希望変更、5秒ごとの自動更新。
- 管理画面（`?admin=1`、Supabase Auth のメール／パスワードでログイン）
  タブ構成は **クール表 / 学生管理 / マッチング / 設定 / 変更履歴**。
- 1次マッチング（抽選）と2次マッチング（空き枠の先取り）
  ロジックは全てサーバ側（`supabase/matching.sql`）にあり、クライアントは
  結果を読むだけで抽選に介入できません。
- 締切到来後の自動確定。学生画面が5秒ごとに `student_matching` を呼ぶため、
  **締切後に最初に学生ページが開かれた時点で自動的に1次マッチングが確定** します。
  スケジューラは使っていないので、誰も開かなければ管理画面のボタンを押すまで
  未確定のままです。

### まだ実行していないこと（最重要）

**`supabase/matching.sql` は Supabase に対して一度も実行していません。**
このリポジトリにはサービスロールキーもマイグレーション実行系も無く、DDL を
実行する手段を意図的に持たせていないためです。適用手順は
`supabase/README.md` を参照してください。

適用前でも画面は壊れません。PostgREST が `PGRST205` / `PGRST202` を返し、
クライアントは `isMissingMatchObject()` でそれを「未確定」として扱い、
マッチング機能が無かった頃とまったく同じ挙動になります（管理画面の
マッチングタブに「どのファイルを実行すればよいか」が表示されます）。

---

## 2. これからの運用体制

| 置き場所 | 役割 | 変更のしかた |
| --- | --- | --- |
| GitHub リポジトリ | `public/` のフロントエンド一式と `supabase/matching.sql` の正本 | コミット／PR |
| 静的ホスティング（Netlify 等） | `public/` をそのまま配信。ビルド無し（`netlify.toml` は `publish = "public"` だけ） | GitHub への push で自動デプロイ。**本番公開は人の判断で** |
| Supabase | DB・RLS・RPC・Auth。業務ロジックの本体 | **SQL Editor に手で貼り付けて実行**。自動適用はしない |
| クラウド版 Claude Code | 実装作業 | リポジトリを開けば `CLAUDE.md` → `AGENTS.md` が自動で読み込まれます |

### クラウド版で作業を再開するときの最短手順

1. GitHub リポジトリを Claude Code（クラウド）で開く。
2. `CLAUDE.md` が `AGENTS.md` を読み込むので、スキーマの実態・命名・
   これまでの判断理由は自動的に前提として入ります。
3. このファイルの「4. 破ってはいけない前提」と「5. 未対応事項」を確認する。
4. Supabase 側の変更が必要なら、`supabase/matching.sql` を編集して
   **人が SQL Editor に貼る**（エージェントには実行させない）。

### ファイルと影響範囲の対応

| ファイル | 変えると何が変わるか |
| --- | --- |
| `public/js/app.js` | 画面・ルーティング・Supabase 呼び出しの全て（単一ファイル） |
| `public/css/style.css` | 見た目のみ。`table.timetable` は `table-layout: fixed` なので列幅に触ると iPad 横画面の収まりが崩れます |
| `public/index.html` | HTML の外枠と CDN からの supabase-js 読み込み |
| `supabase/matching.sql` | **貼り付けて実行するまで DB には反映されません** |
| `netlify.toml` | 配信設定。ビルドステップは無し |

---

## 3. 運用手順（管理者）

### 3-1. 希望調整の受付

1. 管理画面 → 設定タブで `starts_on` / `deadline_on` / `daily_open` /
   `daily_close` を設定する。
2. 学生管理タブで学生を登録し、各学生の個人URL（`?token=...`）を配布する。
   `student_code` は登録済みの行の形式（接頭辞＋ゼロ埋め連番）から自動生成されます。
3. 受付期間中は学生が枠を自由に変更できます（定員超過も許容し、超過セルは
   `st-over` 表示になります）。

### 3-2. 1次マッチング（抽選）

締切は `deadline_on` + `daily_close` を `app_settings.timezone`（未設定・空欄・
不正な値のときは `Asia/Tokyo`）のローカル時刻として判定します。日付・時刻・
タイムゾーンはコードに一切ハードコードしていないので、**確定前に設定を直せば
その最新値が使われます**。

- **確定前に結果を見たい**: マッチングタブの「抽選プレビュー（dry run）」。
  保存されません。
- **締切後に自動確定**: 学生が画面を開いた時点で確定します。操作は不要です。
- **手で確定させたい**: マッチングタブの確定ボタン（確認モーダルあり）。
  取り消せません。
- **一度確定したら二度と抽選し直されません**。`match_rounds.round` が主キーで、
  かつ `pg_advisory_xact_lock` で直列化しているため、同時に何度呼ばれても
  `already_finalized` として保存済みの結果を返すだけです。リロードしても
  結果は変わりません。

確定すると、当選者の枠・氏名は緑のハッチング表示（`td.cell.finalized`）になり、
学生画面の最上部に当選／選外のメッセージが出ます。当選者は以降変更できません。

### 3-3. 2次マッチング

選外になった学生は **残り定員がある枠だけ** 選べます。成功した時点でその枠が
最終確定先として保存され（`match_results.second_slot_id`）、その学生も以降は
変更できなくなります。最後の1席を2人が同時に取ろうとしても、枠ごとの
`pg_advisory_xact_lock` により片方だけが成功します。

### 3-4. テスト実行とリセット

本番データで試す前に、マッチングタブの「テスト確定」を使ってください。
`p_is_test = true` の回だけ `reset_test_matching('RESET-TEST')` で削除できます。
**本番として確定した回は削除できません**（意図的にそう作っています）。

---

## 4. 破ってはいけない前提

これまでの作業でずっと守ってきた制約です。体制が変わっても引き継いでください。

1. **サービスロールキーをこのリポジトリに置かない。** クライアントに
   埋め込めるのは publishable / anon キーだけです（`public/js/app.js` の
   `SUPABASE_ANON_KEY`）。フロントエンドだけで完結する構成を崩さないこと。
2. **RLS を無効化・緩和しない。** アクセス制御は RLS と `admins` テーブル
   （`auth.uid()`）だけで成立しています。anon キーではどのテーブルの行も
   読めず、学生画面のデータ源は `student_snapshot` と `student_matching` の
   2つの RPC だけです。
3. **既存のテーブル・RLS ポリシー・RPC・CHECK 制約・NOT NULL を変更しない。**
   このリポジトリが追加したのは `match_rounds` / `match_results` と
   `supabase/matching.sql` 内の関数・トリガーだけです。
4. **データベースを作り直さない。**
5. **本番公開は人が判断する。** 検証は Deploy Preview で行うこと。
6. **秘密情報をログ・ファイル・コミットに残さない。**
   学生の `access_token` / `student_code` / メールアドレスは、他の学生に
   見える経路へ絶対に出さないこと（`student_matching` は氏名以外を返しません）。
7. `matching.sql` は **再実行可能（idempotent）を維持する。** `create table if
   not exists` / `create or replace function` / `add column if not exists` /
   `drop trigger if exists` で書かれています。

---

## 5. 未対応事項・既知の課題

優先度順。着手するときはこの節を更新してください。

1. **`supabase/matching.sql` の適用（未実施）。** これをやらないと
   マッチング機能は動きません。→ `supabase/README.md`
2. **管理画面の集計が2次マッチングを反映していない。**
   `public/js/app.js` の `matchSummary()` が `outcome === "confirmed"` だけを
   数えているため、2次で枠が決まった学生が「2次マッチング対象」一覧に残り、
   「残り」列にも反映されません。学生画面の表示とサーバ側の定員制御は正しく
   動作します。`second_slot_id` を見るように1箇所直せば解消します。
3. **`after_hours_mode` の許容文字列が不明。**
   `app_settings_after_hours_mode_check` の定義がどのクライアントからも読めない
   ため、設定フォームは「ドロップダウンを実際に動かしたときだけ」この列を
   UPDATE に含めます。正しい2値が判明したら `AFTER_HOURS_CANDIDATES` を
   その2値に置き換えてください。
4. **`student_snapshot` の戻り値の形が未検証**（有効な学生トークンが必要）。
   そのため `normalizeSlot()` は複数のキー名を許容する実装になっています。
5. **自動確定はスケジューラに依存していない。** 締切後に誰も学生画面を開かない
   限り確定しません。確実に時刻で確定させたいなら Supabase の `pg_cron` から
   `run_first_matching(false, false, false)` を呼ぶ方法がありますが、権限と
   運用が増えるため今は採用していません。
6. **`guard_after_first_matching()` の軽微な穴。** 対象の枠の行が `slots` に
   無い場合 `v_capacity` が NULL になり、`v_taken >= v_capacity` が NULL に
   なって定員チェックを通します。元の実装からの既存挙動で、最小変更の方針から
   手を付けていません。
7. **ブラウザでの動作確認ができない環境だった。** 検証は Node の `vm` に
   手書きの DOM と Supabase スタブを載せた使い捨てハーネス（89項目）で
   行っていました。コンテナ内のみで保存していないため残っていません。
   クラウド環境では実ブラウザで確認できるはずなので、そちらを優先してください。

---

## 6. これまでの主な判断理由（作り直さないための記録）

- **抽選を `run_first_matching` と `finalize_first_matching` に分けた理由**:
  管理者チェック付きの関数に `p_auto` のような既定値付き引数を足すと、
  ログイン済みの非管理者がその引数経由でチェックを迂回できてしまうため。
  抽選本体は grant を一切与えず、2つの `SECURITY DEFINER` 関数からのみ
  到達できるようにしています。
- **2次の確定を `outcome` の書き換えでなく `second_slot_id` の追加列にした理由**:
  1次の抽選記録（`slot_id` / `outcome` / `lottery_rank`）を監査用に残したまま、
  `student_matching` が `my_outcome = 'confirmed'` を返せるようにするため。
  結果として既存の画面コードを変えずに済んでいます。
- **`random()` を1回しか呼ばない理由**: 返す値と保存する値が同一の jsonb から
  作られるので、表示と保存が食い違う余地がありません。
- **日付を文字列のまま扱う理由**: `new Date("2026-09-17")` は UTC 深夜になり、
  Asia/Tokyo では「09:00」と表示されてしまうため、`date` 列は
  `fmtDateOnly()` で文字列のまま整形しています。
